events {}
http {
    server {
        listen 80;
        root /app;
        index index.html;
        include /etc/nginx/mime.types;
        location / {
            proxy_pass http://cne-sfia2-project-frontend:5000;
        }
    }
}
